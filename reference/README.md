# reference/

Drop production code into this folder so `component-from-figma` can match the existing
conventions when generating new components.

For BHB, useful references would be:

- The Button SCSS from production: `_assets/scss/general/_formElements.scss` lines 172–1376
- The Button React component: `react-ui/srcFull/core/components/Button.tsx`
- The theme tokens: `_assets/scss/definitions/_theme.scss` lines 168–198

Live in `github.com/danielbarfuss-visma/bhb-design-system`. Copy snippets in as needed —
they don't need to be exact files, just representative.
